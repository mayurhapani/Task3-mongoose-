<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form</title>
  </head>
  <body>
    <h2 style="text-align: center">Add User</h2>
    <form action="\addUser" method="post">
      <table align="center" border="">
        <tr>
          <td>
            <input
              type="text"
              name="username"
              placeholder="Username"
              value="<%= editedUser?editedUser.username:'' %>"
            />
          </td>
        </tr>
        <tr>
          <td>
            <input
              type="email"
              name="email"
              placeholder="Email"
              value="<%= editedUser?editedUser.email:'' %>"
            />
          </td>
        </tr>
        <tr>
          <td>
            <input
              type="password"
              name="password"
              placeholder="Password"
              value="<%= editedUser?editedUser.password:'' %>"
            />
          </td>
        </tr>
        <tr>
          <td>
            <input
              type="number"
              name="age"
              placeholder="Age"
              value="<%= editedUser?editedUser.age:'' %>"
            />
          </td>
        </tr>
        <tr>
          <td>
            <input
              type="number"
              name="mobile"
              placeholder="Moblie No."
              value="<%= editedUser?editedUser.mobile:'' %>"
            />
          </td>
        </tr>
        <tr>
          <td>
            <input type="submit" value="<%= isId?'Edit':'Submit'%>" />
          </td>
        </tr>
      </table>
    </form>

    <table align="center" border="">
      <tr>
        <td>Name</td>
        <td>Email</td>
        <td>Password</td>
        <td>Age</td>
        <td>Moblie</td>
        <td>Action</td>
      </tr>

      <% users.map((user)=>{ %> <% const {_id,username, email,password,age,mobile}= user %>
      <tr>
        <td><%= username %></td>
        <td><%= email %></td>
        <td><%= password %></td>
        <td><%= age %></td>
        <td><%= mobile %></td>
        <td><a href="/deleteData?id=<%=_id%>">Delete</a></td>
        <td><a href="/editData?id=<%=_id%>">Edit</a></td>
      </tr>
      <% }) %>
    </table>
  </body>
</html>
